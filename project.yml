name: ClawPhone
options:
  bundleIdPrefix: com.dsumm
  deploymentTarget:
    iOS: "16.0"
  createIntermediateGroups: true

settings:
  base:
    DEVELOPMENT_TEAM: J7W6QJ77XW
    MARKETING_VERSION: "4.0.0"
    CURRENT_PROJECT_VERSION: "5"

schemes:
  ClawPhone:
    build:
      targets:
        ClawPhone: all
    run:
      config: Debug

targets:
  ClawPhone:
    type: application
    platform: iOS
    sources:
      - path: "ClawPhone"
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.dsumm.clawphone
        PRODUCT_NAME: ClawPhone
        INFOPLIST_FILE: "ClawPhone/Info.plist"
        SWIFT_VERSION: "5.9"
        CODE_SIGN_STYLE: Automatic
        ENABLE_PREVIEWS: YES
    info:
      path: "ClawPhone/Info.plist"
      properties:
        CFBundleDisplayName: ClawPhone
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        NSSpeechRecognitionUsageDescription: "ClawPhone needs speech recognition to convert your voice to messages for Ed."
        NSMicrophoneUsageDescription: "ClawPhone needs microphone access to hear your voice messages."
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: false
          NSExceptionDomains:
            142.132.160.28:
              NSExceptionAllowsInsecureHTTPLoads: true
              NSIncludesSubdomains: true
